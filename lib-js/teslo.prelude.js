(function (teslo) { teslo.prelude = "(def identity (fn (x) x))\n\n(deft (Cons head tail)\n      (Nil))\n\n(def nil (Nil))\n;; (def first (fn (l) (match l (Cons h t) h)))\n;; (def rest (fn (l) (match l (Cons h t) t\n;;                            (Nil) nil)))\n\n(def defm (macro (signature body) `(def ~(first signature) (macro ~(rest signature) ~body))))\n\n(defm (defn signature body) `(def ~(first signature) (fn ~(rest signature) ~body)))\n"; })(this.teslo || require('../'))