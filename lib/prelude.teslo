(def identity (fn (x) x))

(deft (Cons head tail)
      (Nil))

(def nil (Nil))
(def first (fn (l) (match l (Cons h t) h)))
(def tail (fn (l) (match l (Cons h t) t
                           (Nil) nil)))
